function vowelsAndConsonants(s) {
    const vowelArray = [];
    const constantArray = [];
    // const newString = s.split('');
    const vowels = ['a', 'e', 'o', 'i', 'u'];

    for (let element of s) {
        console.log(element);
                if (v = vowels) {
            // return vowelArray.push(vowel);
        }
    }
    // console.log(newString);
}

console.log(vowelsAndConsonants('javascript'));